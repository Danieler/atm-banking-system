const depositSchema = require('./depositSchema');
const withdrawSchema = require('./withdrawSchema');
const transferSchema = require('./transferSchema');

module.exports = {
    depositSchema,
    withdrawSchema,
    transferSchema
};